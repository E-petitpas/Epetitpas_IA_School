# ========================================
# E-petitpas AI School - Backend .env.example
# ========================================

# Server
PORT=3001
NODE_ENV=development

# Database (PostgreSQL)
# Format: postgres://USER:PASSWORD@HOST:PORT/DB_NAME
DATABASE_URL=postgres://postgres:postgres@localhost:5432/ia_school

# Supabase (Server-side)
# URL of your Supabase project (https://xxxx.supabase.co)
SUPABASE_URL=
# Service role key (server-side only, keep secret)
SUPABASE_SERVICE_ROLE_KEY=

# Frontend URL (used for auth redirects)
FRONTEND_URL=http://localhost:3000

# OpenAI (optional: enables real AI answers; otherwise fallback is used)
OPENAI_API_KEY=your_openai_key_here

# CORS (optional overrides)
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Prisma Logging (optional)
# PRISMA_LOG_LEVEL=info
